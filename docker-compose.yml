version: "3"

services:
  web:
    build: .
    ports:
      - "8000:8000"  # Puerto donde gunicorn servirá la aplicación
#    restart: always

  nginx:
    image: nginx:latest
    ports:
      - "80:80"  # Puerto donde Nginx escuchará las solicitudes
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro  # Archivo de configuración de Nginx
    depends_on:
      - web
